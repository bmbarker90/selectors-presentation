{
  "name": "selectors-presentation",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "spectacle-code-slide": "https://github.com/bmbarker90/spectacle-code-slide.git#fix/add-code-theme-class",
    "spectacle-scripts": "1.0.15",
    "spectacle-theme-nova": "^1.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "docs": "rm -rf ./docs && mkdir docs && npm run build && cp -a ./build ./docs ",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "raw-loader": "^0.5.1"
  }
}
